# FastAPI Backend (SQLite)

Run locally:

```powershell
cd fastapi_backend
c:/Users/hp/Desktop/pbl/.venv/Scripts/python.exe -m pip install -r requirements.txt
c:/Users/hp/Desktop/pbl/.venv/Scripts/python.exe -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

API base URL for frontend: `http://localhost:8000/api`
